[package]
name = tkimg
dependencies = ["codecov", "coverage", "nose", "setuptools", "pip", "wheel"]

[tests]
tests = nose

[windows]

[linux]
packages = ["python-tk", "python3-tk", "libtk-img"]

[coverage]
provider = codecov
enabled = True
file = coverage.xml
